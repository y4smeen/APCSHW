import java.util.*, java.io.*;

public class Interval {
    private Random r = new Random();
    private int low, high;

    public Interval(int l, int h) {
	low = l;
	high = h;
    }

    public Interval() {
	low = r.nextInt(100);
	high = low + r.nextInt(100);
    }

    public String toString() {
	String s = "";
	for (int i = low; i < high; i++) {
	    s += 
	}
    }

}
